
 Edit your templates (in templates/ folder) as needed

2. Rebuild the static site:
   python build_static.py

3. Stage, commit, and push:
   git add .
   git commit -m "describe your changes"
   git push

4. Wait ~1 minute — GitHub Pages auto-updates
